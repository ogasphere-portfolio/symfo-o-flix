{% extends 'admin/base_admin.html.twig' %}
{% block title %}Play{% endblock %}

{% block body %}
    <h1>Play</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ play.id }}</td>
            </tr>
            <tr>
                <th>CreditOrder</th>
                <td>{{ play.creditOrder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('play_index') }}">back to list</a>

    <a href="{{ path('play_edit', {'id': play.id}) }}">edit</a>

    {{ include('play/_delete_form.html.twig') }}
{% endblock %}
